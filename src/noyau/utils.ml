let print_bool b =
  print_string (
    if b then
      "true"
    else
      "false"
  )

let contient_texte t(*exte*) l(*iste à chercher*) = false

let couper_texte  t(*exte*) l(*iste à chercher*) = []
